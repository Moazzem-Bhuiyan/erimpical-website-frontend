import CommonPageHeader from '@/component/CommonPageHeader/CommonPageHeader';
import ResponsiveContainer from '@/component/ResponsiveContainer/ResponsiveContainer';
import FilterSection from './_Component/ProductFilter';
import ProductContainer from './_Component/ProductContainer';

export const metadata = {
  title: 'Product',
  description: 'Generated by create next app',
};

export default function page() {
  return (
    <ResponsiveContainer>
      <div>
        <CommonPageHeader
          pageTitle="Product"
          previousPage={{
            pageTitle: 'Home',
            pageRoute: '/',
          }}
        />

        <div className=" flex justify-between  !my-16 flex-col !px-5 md:!px-10 lg:!mx-auto lg:!w-[90%] lg:flex-row lg:gap-x-16 lg:!px-0 2xl:w-3/4">
          <FilterSection />
          <div className="w-full lg:flex-grow">
            <ProductContainer />
          </div>
        </div>
      </div>
    </ResponsiveContainer>
  );
}
